name = "forge-ask-api"
main = "src/index.js"
compatibility_date = "2024-11-01"

# Cloudflare Workers AI — free-tier LLM, no API key needed.
[ai]
binding = "AI"

# Optional secret (higher GitHub rate limits / private repos):
#   npx wrangler secret put GITHUB_TOKEN
